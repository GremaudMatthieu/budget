{
  "name": "gogobudgeto-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "start": "astro dev",
    "deploy": "chmod +x deploy.sh && ./deploy.sh",
    "setup-server": "chmod +x configure-existing-nginx.sh && echo 'Copy configure-existing-nginx.sh to your VPS and run: sudo ./configure-existing-nginx.sh'",
    "configure-nginx": "chmod +x configure-existing-nginx.sh && scp configure-existing-nginx.sh $VPS_USER@gogobudgeto.com:/tmp/ && echo 'Script uploaded. Run: ssh $VPS_USER@gogobudgeto.com sudo /tmp/configure-existing-nginx.sh'",
    "fix-permissions": "chmod +x fix-permissions.sh && ./fix-permissions.sh"
  },
  "dependencies": {
    "astro": "^4.15.0",
    "@astrojs/tailwind": "^5.1.0",
    "tailwindcss": "^3.4.0",
    "@astrojs/sitemap": "^3.1.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}